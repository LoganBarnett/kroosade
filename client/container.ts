/**
 * A container is a generic container of any kind. Generally it will be a div
 * but it could be anything. Using this allows us to integrate our styling with
 * how we generally use components, but there's no other great advantage over
 * using this vs. just a flat element.
 *
 * Maybe we don't need it at all?
 */
